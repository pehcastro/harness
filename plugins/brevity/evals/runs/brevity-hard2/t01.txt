Case is not the cause. The keys are `'1'`, `'2'`, `'3'`, all digits, so `/users/A` 404s because no user has that id. A case-insensitive lookup would change nothing.

Do you want a user with id `A` seeded, or was `/users/A` a stand-in for a real id?