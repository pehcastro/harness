These conflict: test/app.test.ts:14 asserts `/users/1` returns 200 with Ada. An unconditional 500 makes that test fail, so one of them has to go.

Which do you want?
1. `/users/1` always 500s: I delete the existing 200 test.
2. The 500 is an error path (e.g. store lookup throws), and I add a `try/catch` in the route plus a test that forces the throw. The 200 test stays.